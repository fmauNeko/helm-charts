suite: deployment image resolution
templates:
  - deployment.yaml
tests:
  - it: uses appVersion when image.tag is empty
    set:
      image.tag: ''
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: nginx:1.27.0
