apiVersion: v2
name: netbird
description: Self-hosted NetBird mesh VPN — management, signal, relay, and dashboard.
type: application
version: 0.2.7
# renovate: datasource=docker depName=netbirdio/management versioning=docker
appVersion: '0.73.1'
